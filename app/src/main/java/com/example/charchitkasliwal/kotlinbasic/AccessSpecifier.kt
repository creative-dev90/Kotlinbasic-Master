package com.example.charchitkasliwal.kotlinbasic

/**
 * Created by charchit.kasliwal on 08-06-2017.
 */
class AccessSpecifier {

    // Terminology  Not  Access Specifier  its a Visibility Modifiers
    /**
     * 1) Public :- public is used by default, which means that your declarations will be visible everywhere
     * 2) Protected :- protected is not available for top-level declarations
     * 3) private :- it will only be visible inside the file containing the declaration
     * 4) Internal :- it is visible everywhere in the same module
     */

}